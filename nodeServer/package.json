{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "This is a node server for let's Chat appication",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bhoomika",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.6.2"
  }
}
